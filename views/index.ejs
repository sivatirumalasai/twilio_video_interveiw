<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8" />
  <title>Twilio Video Demo</title>
  <!-- Twilio Video CDN -->
  <script src="https://sdk.twilio.com/js/video/releases/2.15.2/twilio-video.min.js"></script>
  <script src='https://kit.fontawesome.com/a076d05399.js' crossorigin='anonymous'></script>
  <style>
    #video-container {
      width: 100%;
      overflow: hidden;
      -webkit-column-count: 3;
      -webkit-column-gap: 1.875em;
      -webkit-column-fill: auto;
      -moz-column-count: 3;
      -moz-column-gap: 1.875em;
      -moz-column-fill: auto;
      column-count: 3;
      column-gap: 1.875em;
      column-fill: auto;
      display: flex;
    }

    .participant {
      overflow: hidden;
      -webkit-mask-image: -webkit-radial-gradient(white, black);
      position: relative;
      border-radius: 8px;
      box-sizing: border-box;
      margin-bottom: 1.875em;
      -webkit-column-break-inside: avoid;
      -moz-column-break-inside: avoid;
      column-break-inside: avoid;
    }

    .app {
      padding: 1%;
      width: 100%;
      box-sizing: border-box;
      font-weight: bold;
      font-size: 1.5em;
      background-size: cover;
      position: fixed;
      overflow: scroll;
      top: 0;
      bottom: 0;
      left: 0;
    }

    #leaveRoom {

      margin: 0 auto;
      margin-top: 25px;
      margin-left: 0;
    }

    input {
      padding-bottom: 5px;
      border: none;
      outline: none;
      border-bottom: 2px solid #067486;
      margin-bottom: 25px;
      width: 300px;
      font-size: 1em;
      text-align: center;
      border-radius: 0;
      background-color: rgba(255, 255, 255, 0);
    }

    button {
      background-color: #067486;
      font-weight: bold;
      display: block;
      margin: 0 auto;
      color: white;
      border: none;
      padding: 15px;
      border-radius: 5px;
      width: 200px;
      cursor: pointer;
      font-size: 1em;
    }

    .lobby {
      margin-top: 100px;
      text-align: center;
      font-size: 1.25em;
    }

    .identity {
      position: absolute;
      left: 0px;
      bottom: 15px;
      color: white;
      padding-left: 15px;
      box-sizing: border-box;
      z-index: 50;
    }
  </style>
</head>

<body>
  <div class="app">
    <div class="lobby" id="lobby_div">
      <form id="room-name-form">
        <input type="hidden" name="room_name" id="room-name-input" value="<%= room_id %>">
        Enter a Name to join: <input name="user_name" id="user-name-input" />
        <button type="submit">Join Room</button>
      </form>
    </div>
    <div class="room" id="room_div" style="display: none;">
      <div id="video-container"></div>
      <button id="leaveRoom">Leave Room</button>
    </div>
  </div>
  <script src="/main.js"></script>
</body>

</html>